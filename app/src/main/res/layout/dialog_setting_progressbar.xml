<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingStart="20dp"
    android:paddingEnd="20dp"
    android:paddingTop="12dp"
    android:paddingBottom="12dp">

    <TextView
        android:textColor="?attr/dialogTitleTextColor"
        android:text="设置最大缓存大小"
        android:textSize="22sp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"/>

    <SeekBar
        android:paddingStart="0dp"
        android:paddingEnd="0dp"
        android:layout_marginTop="16dp"
        android:id="@+id/dialogDownloadProgressSeekBar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        tools:progress="50" />

    <TextView
        android:id="@+id/dialogDownloadProgressText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:textAlignment="center"
        android:textColor="?attr/appTextNormalColor"
        tools:text="进度 : 50 / 100" />

    <Button
        android:id="@+id/dialogCacheSizeConfirmButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:text="@string/settingDialogConfirm"
        android:textColor="?attr/appTextNormalColor" />

</LinearLayout>