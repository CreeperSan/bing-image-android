<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="?attr/selectableItemBackground"
    android:clickable="true"
    android:focusable="true"
    android:padding="16dp">

    <ImageView
        android:id="@+id/itemSettingSimpleIcon"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:tint="?attr/appTextHintColor"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:src="@drawable/ic_info" />

    <TextView
        android:id="@+id/itemSettingSimpleTitle"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="16dp"
        android:textColor="?attr/appTextNormalColor"
        android:textSize="14sp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toEndOf="@id/itemSettingSimpleIcon"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="我是设置标题" />

    <TextView
        android:id="@+id/itemSettingSimpleContent"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="16dp"
        android:layout_marginTop="4dp"
        android:ellipsize="end"
        android:maxLines="3"
        android:textColor="?attr/appTextHintColor"
        android:textSize="14sp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toEndOf="@id/itemSettingSimpleIcon"
        app:layout_constraintTop_toBottomOf="@id/itemSettingSimpleTitle"
        tools:text="这里是一些说明" />

</androidx.constraintlayout.widget.ConstraintLayout>