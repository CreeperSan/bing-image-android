<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
                                             xmlns:tools="http://schemas.android.com/tools"
                                             xmlns:app="http://schemas.android.com/apk/res-auto"
                                             android:background="#FFFFFF"
                                             android:orientation="vertical"
                                             android:layout_width="match_parent"
                                             android:layout_height="match_parent">



    <ImageView
            android:id="@+id/mainLeftIcon"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            android:src="@drawable/ic_main_drawer"
            android:padding="12dp"
            android:layout_width="48dp"
            android:layout_height="?attr/actionBarSize"/>

    <ImageView
            app:layout_constraintBottom_toBottomOf="@id/mainLeftIcon"
            app:layout_constraintTop_toTopOf="@id/mainLeftIcon"
            app:layout_constraintEnd_toStartOf="@id/mainRightIcon"
            app:layout_constraintStart_toEndOf="@id/mainLeftIcon"
            android:layout_gravity="center"
            android:src="@drawable/ic_main_title"
            android:layout_width="0dp"
            android:padding="6dp"
            android:layout_height="?attr/actionBarSize"/>

    <ImageView
            android:id="@+id/mainRightIcon"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            android:src="@drawable/ic_main_search"
            android:padding="12dp"
            android:layout_width="48dp"
            android:layout_height="?attr/actionBarSize"/>


    <android.support.v7.widget.RecyclerView
            android:id="@+id/mainRecyclerView"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintTop_toBottomOf="@id/mainLeftIcon"
            android:layout_width="match_parent"
            android:layout_height="0dp"/>

    <Button
            android:visibility="gone"
            android:id="@+id/mainAdd"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"/>

    <TextView android:visibility="gone" android:layout_width="wrap_content" android:layout_height="wrap_content" android:id="@+id/mainText"/>

</android.support.constraint.ConstraintLayout>